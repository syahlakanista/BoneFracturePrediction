# BoneFracturePrediction
This repository contains a simulation of a Convolutional Neural Network (CNN) implementation in a Streamlit-based web application. The project includes the main Streamlit app, a trained CNN model (.h5), and a virtual environment to support inference and system evaluation.
